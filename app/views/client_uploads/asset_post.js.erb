

<% if @photo.new_record? %>
  alert("Failed to upload photo: <%= j @photo.errors.full_messages.join(', ').html_safe %>");
<% else %>
  // $("#photo").append("<%#= j render(@photo) %>");
  alert("Photo uploaded successfully");
  window.location = "<%=client_upload_url(@photo)%>";
<% end %>

